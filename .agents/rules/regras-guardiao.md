---
trigger: always_on
---

# WORKSPACE SHIELD: MINHA ESCALA PRO (SINERGIA COM V3.6)

Este Workspace é um ambiente de ALTA SEGURANÇA. Você (Agente Antigravity) opera sob as leis estritas do seu `SYSTEM PROMPT v3.6`. Este documento complementa sua programação com restrições físicas de ambiente.

## 1. RECONHECIMENTO DE AMBIENTE (SINGLE-FILE)
- Você está restrito ao diretório: `C:\Users\jefer\Desktop\MINHA_ESCALA_VERSAO_TABLET`
- O ÚNICO arquivo editável deste projeto é o `index.html`. 
- É expressamente PROIBIDO rastrear, ler ou sugerir modificações em qualquer arquivo externo, criar arquivos `.css`/`.js` ou fragmentar a arquitetura.

## 2. TRAVA DE EXECUÇÃO (O MÉTODO GUARDIÃO)
- Todo comando de alteração chegará até você no formato de script **PowerShell + injeção `node -e`**.
- Se um comando de edição chegar até você pedindo para usar "Accept Changes", edição direta no editor de texto, ou substituição manual: REJEITE IMEDIATAMENTE.
- Você só tem permissão para alterar o `index.html` executando scripts silenciosos no terminal.

## 3. PROTOCOLO DE CEGUEIRA SELETIVA (PROGRESSÃO SEMPRE, REGRESSÃO ZERO)
- Ao abrir este Workspace, você está "cego" para tudo que não for o escopo exato do comando recebido.
- Você não analisa o código em busca de "melhorias", "otimizações", ou "refatorações" de variáveis, funções de PDF, Supabase, OneSignal ou layouts validados.
- O que funciona, é INVISÍVEL e INTOCÁVEL para você.

## 4. GATILHO DE ABORTO (FAIL-SAFE)
- Se a execução do `Select-String` (Busca de Âncora) não encontrar a linha exata, você está PROIBIDO de tentar "adivinhar" onde o código deve ser injetado.
- Ação exigida em caso de erro: ABORTE a operação imediatamente e responda EXATAMENTE com: "❌ Âncora não encontrada. Operação abortada para evitar regressão. Solicito novo script ao Guardião."